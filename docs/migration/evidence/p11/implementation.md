# P11: fail generic streamed verification on incomplete output

External read-only proposal; no source/metadata changes or tests. Inspected current P10 worktree runner and execution tests. P10 fixes reference grouping but deliberately leaves this independent defect open.

## Concrete defect and minimal scope

`tools/test-runner/process.mjs` prefixStream accepts only maxOutputBytes from each stdout/stderr stream and emits `[suite] [output truncated after N bytes]`. Its truncation flag never reaches runStreaming’s result. Child exit0 can therefore yield status=passed,exitCode=0,signal=null despite missing test output. `executeSuites` forwards that status, and `buildReceipt` marks the overall receipt passed unless a suite status is failed. The existing literal `process execution terminates hangs and bounds noisy output` even asserts this wrong passed result for1000bytes with a32byte cap.

Own only `tools/test-runner/process.mjs` and `tests_js/test-runner-execution.test.mjs`, plus normal P11 evidence planning/audit/DAG/manifests/transition paths. One production module, no new helper initially needed; current process module is well below500lines and execution test is approximately250. Preserve every existing test name and all unrelated assertions, explicitly correcting the noisy-success assertion. Do not change matrix caps, P10 grouping, local-checks/process, generic receipt schema, runCapture, compiler/native helpers, consumer pins or accepted reference logs.

## Result and lifecycle contract

Preserve raw per-stream byte accounting: stdout and stderr each have the existing independent limit, default2MiB; exactly N bytes is allowed, N+1 triggers failure. Prefix and diagnostic formatting do not count as child payload. Keep the useful single truncation diagnostic per offending stream and preserve accepted prefix bytes/UTF8 decoder behavior. No arbitrary output beyond the accepted prefix is retained. An empty stream with limit0 is valid; its first byte exceeds the cap. Do not raise a cap to avoid failure.

On first detected overflow, latch terminal reason `output-limit`, stop accepting success, initiate the existing owned termination path (TERM then bounded KILL/forced resolution), and eventually return `{status:'failed',exitCode:130,signal:'output-limit',durationMs}`. This matches the existing local-checks reason/code convention while keeping its implementation unchanged. If the child has already exited0 but buffered output reveals overflow before close, failure still wins. A timeout previously latched remains timeout124; a later timer must not overwrite output-limit. Never restart cleanup timers on subsequent chunks or the other stream. Preserve ordinary nonzero exits, spawn errors and timeout behavior.

Refactor the existing timeout cleanup into a small local common stop path only if needed; no new lifecycle framework. Ensure close cannot settle success while overflow cleanup is pending, including parent exit while the existing process group still has a descendant. Keep the current bounded cleanup semantics and their known limitations: this task does not prove escaped-descendant containment, PID-reuse safety or a host lease (P04 remains open). Do not add stale PID signalling mechanisms. The fixed owned subprocess regressions verify the existing supported process-group topology only.

No receipt schema change is necessary: a failed runStreaming result becomes a failed executeSuites row and a failed buildReceipt status using current code. Prove that full path with actual subprocess results, not only a hand-constructed failed row. Receipt privacy remains unchanged: no command/output/environment copied into JSON. Existing generic skipped/deferred semantics and unbounded runCapture concerns are outside this task.

## Fixed regression witnesses

Retain all nine current execution-file literal names, including formatter behavior and the two POSIX timeout/tree tests. Correct only the noisy scenario’s status expectation and add exact output-limit code/reason assertions. Add these four literal names in the same registered command:

1. `P11 streamed output enforces independent stdout and stderr byte boundaries` — actual owned Node child cases for empty/zero cap, N-1,N,N+1 on each stream; both streams independently at N still pass (not an invented combined cap). Exercise one large write and delayed smaller writes crossing the boundary, empty lines, partial final lines and multibyte UTF8 split writes. Verify exact accepted ASCII prefix, bounded raw-input accounting and preserved decoder behavior rather than equating formatted-output size with the input cap.
2. `P11 output overflow cannot pass after zero exit or competing termination` — stdout-only/stderr-only/both overflow with child voluntarily exiting0; repeated chunks/dual overflow yield one latched failure and at most one marker per stream. Include an overflowing child that keeps running and a timeout-after-overflow case; output-limit remains the first reason. Retain separate existing timeout assertions. Use bounded child programs and event ordering, not optimistic close timing.
3. `P11 output overflow retains cleanup for an owned surviving descendant` — fixed child/grandchild handshake ensures descendant exists before noisy output; TERM-ignoring descendant and early direct-parent exit variants. Capture only owned fixture PID/handles, confirm supported descendant cleanup, and remove temp state in finally. Native Windows tree semantics stay visibly deferred; this POSIX-only witness must not claim Windows acceptance. Existing supported-host cell must have zero skips.
4. `P11 incomplete output fails suite receipts without leaking captured data` — executeSuites with an actual overflowing zero-exit command and a passing neighbor, then buildReceipt/writeReceipt/read back. Assert matrix-order results, failing row and overall failure despite the neighbor, and unchanged omission of sentinel command/output/environment data. Exact-at-limit companion succeeds. No repository test suite or native addon is launched.

Use independently specified expected limits/statuses and child programs; do not merely check a formatter’s internal flag. A pre-fix version must fail the corrected noisy assertion and at least the real overflow→receipt scenario. Retain any genuine before-fix observation if captured under authorization, but do not fabricate one.

## DAG and immutable capture

Propose inert audit package P11/parentCI. P11.I depends on P10.V and P03.retry2.V (the accepted matrix isolation and reporter/process predecessor); P11.V depends on P11.I/P01.V. Append P11.V to the never-frozen final testing gate T05.I while preserving every current dependency; verify eligibility at the actual activation boundary. If T05.I is no longer eligible, stop for a reviewed integration route rather than alter frozen assignments. P04 is not a dependency: no host-lease or native-recovery capability is claimed.

Use one existing registered command `node --test tests_js/test-runner-execution.test.mjs` with thirteen exact names (nine retained plus four above),180seconds/1MiB. Freeze actual names and current environment after review; darwin node-local can exercise the existing POSIX witnesses without skips. No new matrix registration. Normal source commit and exact immutable capture follow independent source review. Root owns host-heavy scheduling and acceptance; tests are small owned subprocesses, but the cleanup cases must run serially relative to other host-heavy checks.

Bind the actual recursive local import closure of the execution test (execute/process/receipt/matrix/patterns, local-checks process/git and CLI imports), package/lock and current matrix as read-only manifest inputs; include the process module as an owned existing input. Preserve P03’s historical source identities: changed runner/test bytes may conservatively disable live focus until a separate reviewed recipe update; never renew hashes automatically to regain boundedness.

Before activation, review exact semantics and all13names/cell limits; after implementation run the focused command, size and whitespace checks, then independently validate the committed raw logs/results. A full affected run is integration evidence separately scheduled by root, not needed to invent broad P11 source ownership. Until this fix is accepted, continue rejecting affected/full integration output containing a truncation marker even if its generic receipt reports passed.
