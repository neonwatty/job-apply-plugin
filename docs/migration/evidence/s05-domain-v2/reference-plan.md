# Store path and cache domain reference — corrected host

Freeze the same 32 original-Python domain observations using the existing resume
timestamp reference entry point. Preserve its callback bodies and refusal test;
spell its four profile registrations literally and append five domain tests.
Keep managed-observation and historical baseline files byte-identical.

This append-only preparation supersedes the earlier unactivated Store preparation,
whose proposed host conflicts with a historical baseline source pin. The DAG and
UI preparations remain unchanged. Include the timestamp driver's import closure
and retain complete raw observations, typed constructors, provenance, and bounded
local Mac commands. No matrix changes or TypeScript Store activation are included.
