# M01 pure automation request preparation

External review proposal at 4463385449dddd340d347836895c0850a2f0919d; no source activation or acceptance.

Prepare the three JavaScript-only request constructors from workspace/lib/api.js
in a dependency-free TypeScript leaf. The original35 presentation helpers are
already ported and remain unchanged. M01 is an inert preparation child of UI0;
M01.I depends on P00.V, M01.V on M01.I/P01.V, and its accepted V is appended to
never-frozen U02.I while retaining U01.V and U02.R. No existing gate is waived.

Implementation scope is exactly automation-request.ts, its emitted .js, and the
existing workspace_helpers.test.mjs entry named in the JSON. Preserve all14
original callback bodies and make their existing test import directly visible
to discoverTestIds. Append the three exact differential literals from the JSON;
17 total, no nested registration or test-matrix change.

The new fixtures preserve the two ordinary owner-beta reference examples and
add bounded coercion, invalid revision, URI encoding, copy/order, prototype,
getter and thrown-identity cases. Separate equivalent factories run against
the immutable original API and compiled leaf. No original owner-beta scenario
is imported or launched. Returned requests remain data; no request is sent.

The retained support dynamically imports workspace/app.js. Its actual static
bootstrap/features closure is input-bound even though ordinary Node has no
document and does not activate the app. Reject redirected test roots. The
original callbacks use mocked fetch and in-process deferred promises. The
proposed existing-command budget is30seconds/1MiB; this is not an observed pass.

All source/runtime/test files remain <=500 physical lines; the current witness
is220 lines. Coordinator emits runtime and owns the source-catalog and review
metadata; those paths must be in the final reviewed manifest scope. Independent
freeze review, actual fresh immutable bindings and ordinary hooks precede
source execution and acceptance. All existing U02 obligations remain in force.

No source or tests were executed to prepare these external files. The JSON
contains exact case groups,17 names, input hashes, DAG refinement and checks.
