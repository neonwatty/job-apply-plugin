# P09 captured-successor currentness

Draft for independent review; no source acceptance or metadata application. Start from clean b303658392cb99dcd018a395ef4629dbd9a639bd after restoring only failed uncommitted P03 activation. Preserve every P03 retry preparation, manifest, transition and original raw capture unchanged. P03 retry activation remains absent. This is a tooling capability correction, not a waiver or retroactive passing result.

## Exact defect and scope

P03.R owns matrix hash49fe9fdc, while original pending P03.I owns/input-binds that hash and changes it to1e64e3c3 at24615b. Retry input correctly adopts1e64e3c3. Retirement removes original I from pendingSuccessors. The current receipt validator cannot connect the accepted R through the validated retired capture to retry, despite exact source adoption being proven by lineage. Only config/test-matrix.json is stale in R's owned files.

Own three production modules: load-task-lineage.mjs, load-task-evidence.mjs and task-receipts.mjs under tools/migration. Their current physical lines are330,291,157. Own existing receipt test134lines, lineage lifecycle test129lines, lineage Git support257lines and task support201lines. Seven code/test files total; three production modules. Every file remains<=500, no exceptions/extractions or additional modules. Existing imports, schema validators, Git IO, TAP parser and structural package validator remain shared unchanged primitives.

## Trusted bridge facts and validation

No new receipt-supplied authorization or catalog schema. After full existing lineage validation, load-task-lineage exports capturedSuccessors, a Map keyed by retired task ID. Each entry binds its exact frozen manifest hash, validated activation, captured subject/tree/evidence SHA, exact captured file hashes, and unique replacement task ID plus activation. Both-role mappings must remain explicit: a null review attempt may use only the already validated shared implementation capture for its exact adopted paths. It must never create evidence of a review run.

load-task-evidence converts these into currentness facts using immutable Git and manifests, not caller assertions. The bridge is path-specific. Require old and replacement ownership of that path; old input equal the predecessor expected hash; captured owned-file hash equal the replacement input at replacement.base; unchanged adoption at replacement base; all original ordinary input/ownership checks; predecessor subject ancestor of original base; original activation ancestor of captured subject, evidence and preparation; capture preparation before replacement base; transition and replacement activation ancestry. Both original and replacement must retain the actual predecessor dependency in their own/effective approved DAG ancestry. Product-to-audit substitution remains prohibited; no structural product readiness or ownership release is granted.

Receipt currentOrSuccessor may traverse accepted predecessor→validated captured retired attempt→active replacement. A retired capture is a hash transition, never a candidate receipt or accepted task. Do not insert retired IDs into pendingSuccessors or manufacture changedPaths. At the active pending endpoint require actual current bytes equal the adopted hash or ordinary validated pending scoped drift after activation. Even adoption with zero postactivation source diff makes currentAcceptance open and all current accepted sets empty when the bridge is necessary. Historical immutable receipts remain valid. A fresh independently reviewed replacement receipt may close the bridge only through all existing subject, input, dependency, TAP and review checks. A later real edit uses the ordinary existing successor chain.

Compose multiple uniquely linked captured replacements transitively, tracking visited IDs and path/hash states; reject cycles, missing links, foreign activations, competing successors, dropped inputs, null absent source pretending to adopt a file, or a mismatched intermediate hash. Final receipt batch validation still controls everything: one invalid receipt/bridge unlocks nothing. Do not permit captured logs to satisfy replacement outcomes, loosen existing copied-log rejection, accept retired task receipts, or globally repopulate acceptedPackages from historical facts. Missing bridge facts preserve old fail-closed behavior.

## Independent evidence

Preserve all10 existing receipt witnesses and4 existing lineage lifecycle witnesses unchanged in assertion strength. Add3 pure receipt tests and2 real lifecycle tests with exact names in audit: total19names,2cells,180s/1MiB each, zero required skips/failures/cancel/TODO/timeouts.

Pure tests construct independent predecessor expected hash, original input/captured hash and replacement input/current hash and check valid zero-diff adoption, pending edits, fresh completion, transitive two-retirement chains and cycles. Rebind one fact at a time to reject forged capture/hash, wrong/dropped ownership or input, wrong ancestry, lost dependency, product-to-audit substitution and invalid final batch. Verify retired IDs never enter accepted/current historical receipt sets.

Actual disposable Git lifecycle reproduces accepted reference R owning a file, original I frozen against R's bytes, actual I edit/capture without receipt, capture pin, retry preparation/transition/activation, zero-source-diff activation, fresh replacement subject/log/evidence/review receipts. Check each real staged snapshot including dependency-linked snapshot; preserve owner HEAD/index/status. At activation R remains historically valid but all current accepted sets empty. New independently reviewed replacement closes continuity without granting retired I acceptance. Include an original unaccepted nonzero outcome or wrong-name capture honestly; no fabricated diagnostic. Include a real second captured retirement H0→H1→H2 before final acceptance, with first/second capture removal and cycle negatives; a pure-only transitive chain is insufficient. The predecessor fixture is an actual accepted audit reference A.R sharing this exact owned source from genesis; retain product-to-audit rejection separately, without changing product handoff semantics. Negative permutations alter real frozen bindings with consistent hashes so they reach the bridge guard rather than unrelated syntax checks. No receipt-supplied commands executed.

## Metadata lifecycle and follow-on P03

Append P09.I/V to current340-task DAG for342. I depends P07.V and P08.V; V depends P09.I and P01.V. Strengthen never-frozen T05.I with P09.V while preserving all existing dependencies and all accepted IDs. Author/owner jsonl_review; independent reviewer hooks_audit. Inert audit only, CI parent remains open. Read-only generator produces documents→manifests→transition→activation bundles on successive actual clean commits. Root independently reviews/applies and normal-commits each. Actual manifest closure follows all existing owned code/test local imports recursively, package/toolchain locks, compiler configuration, source-size baseline and matrix; no invented hashes or future commits.

After P09 acceptance, create new versioned P03 retry preparation paths based on accepted tooling, with newly bound inputs/DAG and actual transition review. Never overwrite the old pinned but unactivated P03 retry manifests or transition. This task alone does not activate P03 or mark any migration product complete.
