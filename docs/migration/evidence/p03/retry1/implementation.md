# P03.retry1 revised external contract draft

Status: actual freeze-review candidate at553c87d02a9bcad4a2bf5aa22f3ae7c60497c585. No repository edits, freezes, metadata application, tests or captures in this preparation. The three external v2 drafts supersede earlier external retry proposals; all real original manifests, normative tables, raw logs and capture bytes remain immutable. Root and hooks_audit must review the final integrated-base table and this decided tier boundary before freeze.

## Preserved evidence and dependency graph

Preserve original P03.I/V unaccepted identities, matrix-only subject24615b8298f7868791517d303322ea4ebe1c4a61, executionBaseb0954e4c1e1238a6813a28af63124078c870bac7, actual30/43 capture and four original logs exactly. Accepted P03.evidence.R owns the old exact raw-evidence formatting treatment; no new raw-log whitespace exemption. Pin original capture after actual integrated evidence checkpoint and before retry manifest.base. Preserve full original owned-path history through that checkpoint; do not hide later edits under a planning boundary. Retire I with its actual capture attribution; V remains unexecuted with the validator's explicit shared-scope coverage, never a fabricated V run.

Accepted P08 and S08 are integrated at553c87d02a9bcad4a2bf5aa22f3ae7c60497c585. This is the final original-evidence checkpoint; capture pin must precede retry base selection. Effective DAG is then340 after additive P08; retry adds two identities for342 (derive and assert actual lineage tip rather than stale338 assumptions). Preserve P03.evidence.R, P08.I/V and every accepted identity. Retry I retains original ancestor obligations and explicitly depends on P03.evidence.R and P08.V; retry V retains matching retry I and P01.V. Refine only the verified never-frozen direct consumers T04.I and T05.I from P03.V to retry V, retaining T05.I's newly added P08.V dependency. Any changed consumer set or frozen consumer stops preparation for review. No accepted identity is aliased or rewritten.

## Final ownership and directional implementation

Retain every original v2 allowed path and all old prepared documents. Production files are exactly eight: tools/local-checks/policy.mjs, run.mjs, consumer-graph.mjs, graph-syntax.mjs, focused-contracts.mjs, focused-closure.mjs, tools/test-runner/process.mjs and tools/local-checks/process.mjs. Syntax/contract helpers feed graph closure; policy consumes the checked closure; run orchestrates selection/cache/execution; test-runner/process supplies a lower formatting primitive; local-checks/process may import that primitive while retaining its distinct supervision. Neither imports policy. All<=500 physical lines; no extraction exception or ninth module.

Tests retain local_checks_policy.test.mjs, local_checks_run.test.mjs, test-runner-selection.test.mjs and local_checks_graph_support.mjs. Add ownership of existing registered test-runner-execution.test.mjs for reporter proof and narrowly specified declaration adjustment. Git helper/test remain readonly. Own config/test-matrix.json for precisely the new reference registrations. New retry owned documents are implementation.md, reference-consumers.json, implementation-audit.json and both manifests under docs/migration/evidence/p03/retry1. DAG/capture/transition are exact pin-only preparation records. Fresh five log paths are in the same retry directory.

Preserve all43 original names, all7 original requirements and four original command cells unchanged in identity/budgets/required outcomes. Add one reporter requirement and one cell with all8 existing execution test names plus one new literal: stream prefixes empty lines without trailing spaces and preserves nonempty payloads. Total52 names,8 requirements,5 cells; each180000ms/1MiB/node-local/node-tap13, no required skips/fails/cancel/TODO/timeouts. Native root execution policy tests can use owned synthetic execution fixtures to prove selection/nonreuse without rerunning every native suite inside the tooling unit cell; final heavy integration remains separately required.

## Updated normative consumer closure

Historical P03-post-s08-consumers.json and P03-post-s08-inventory.md bind72598b7e1667338d8e6a4100c7812aa366cbb7ad. Preserve them unchanged. The refreshed P03-post-p08-consumers.json and P03-post-p08-inventory.md independently bind553c87d02a9bcad4a2bf5aa22f3ae7c60497c585 after actual accepted P08/S08 integration:342 candidates,247 broad syntax call records and141 URL records. Candidate digest is507d47e6fdf587c4394a43ae8ddab64946a9f76c83686ff5839170d7b725cb0c. All final paths/bytes/calls/closures were re-evaluated, not just the subject label. Historical342/234/140 and older327/152 counts remain distinct census records, never interchangeable measures.

Normative exact repository path: docs/migration/evidence/p03/retry1/reference-consumers.json. Its proposed bytes are the external P03-retry1-v2-final-consumers.json, reconciling the final census with the unchanged approved six-native-root policy and five future registrations. It is a required immutable manifest input. The generator verifies current342 source hashes plus complete call/URL census equality against the final integration and rejects changed source while documents/manifests are prepared. Independent semantic edge/closure review remains required; syntax census itself does not prove a safe terminal.

Do not permanently bind every product source as an individual manifest input. The exact table is normative historical data; runtime analysis re-evaluates all current tracked bytes/callers before selection and before cache reuse. Existing tool/test transitive input closure, compiler/package locks and final P08 source are separately input-bound as appropriate. No future source change may inherit a terminal classification from an unchanged call expression alone.

Retain all six focused rule IDs and their explicit source/test-input/shared-contract classifications. Post-S08 numeric runtime and shared-type closure includes all three point tests; legacy parser/serializer and Store validation also reach point compatibility. The finite point parser/codec/serialization/value paths explicitly listed by the normative table are part of local-typed-json, including the new facades and shared engines. Their concrete reverse closures and shared-type escalation are required; all unlisted new seams retain conservative fallback. This is a reviewed finite expansion inside the same six rule IDs, not a seventh rule or general prefix wildcard. Browser resume consumer is real and heavy. posix_flock is a module-URL companion through shared support; no false numeric behavior claim. Shared type rules include source+emission+transitive type consumers rather than erased runtime-only edges.

A newly resolved P08 data edge is mandatory: tests_js/migration_test_bindings.test.mjs reads typed_json_oracle.test.mjs, raw_json_numeric.test.mjs and store_validation_ts.test.mjs via its finite filename loop and new URL(file, import.meta.url). These are real source-byte consumers. The table records all three edges and the reader in numeric/typed-json/validation input companions and conservative transitive module closure; existing all-fast selection does not excuse an omitted dependency. Mutate one read source's literal registration while the reader remains unchanged and prove it is selected; do not classify this read as a terminal fixture.

All tracked eight JS/TS suffix candidates, including outside conventional roots, remain in scope. Resolve literal/default/named/reexport, source/emitted and type/module-URL edges, finite browser/copy/child mappings, aliases, module-relative extension/index forms, and caller provenance. Symlink/external/unresolved targets and computed children deny focus or localize only through the explicitly reviewed conservative test-root obligation. Terminal declarations bind exact callsite classification AND containing-file hash; unchanged call text with changed alias is invalidation. New/changed consumers cannot silently escape. Re-evaluate discovery and closure before cache lookup even with empty changes. Analyzer/contract/toolchain identity changes invalidate reusable nonnative results; every selected test must have a successful matching result.

## Six native roots: conservative execution, not a terminal proof

The closed six are posix_flock.test.mjs, exclusive_file_lock_ts.test.mjs, exclusive_file_lock_fault_ts.test.mjs, jsonl_lock_integration.test.mjs, heavy_run_lease_reference.test.mjs and posix_timestamps.test.mjs. Use existing native-posix-lock and native-posix-timestamps group identities/platform restrictions. Preserve all surrounding ordinary module/type/support/URL edges; do not drop them when adding the roots. Deduplicate concrete tests against local-dependent-integration so each required root runs once per attempt, without reducing either obligation.

Approved constraints: recompute caller closure BEFORE cache and NEVER cache-skip those cells even with empty changes/full fallback; preserve ordinary edges, isolation, serialization and platform deferrals. This does not prove compiler selection, environment, headers/SDK or artifact immutability. No native terminal annotation or execution acceptance may be inferred from repository bytes alone. New unrecognized callers expand required root closure or deny focus; a hardcoded six list must not silently survive new consumer files.

Tier boundary: ordinary commit/fast remains fast and makes no native/deep claim. Explicit deep executes all six on supported hosts, including empty-change runs. A heavy push may reuse valid nonnative results only after fresh native roots run serially within that invocation and under coordinator host scheduling; no receipt may count old native passes as fresh. Unsupported platforms are explicit unresolved/deferred native cells, never green native acceptance.

Decided docs-only PUSH boundary: preserve the current documentation-only push fast path and ordinary precommit fast path, with no native/deep acceptance claim. Source/graph classification must occur before granting that path so changed code, new consumers, symlinked documents or a changed policy cannot masquerade as documentation. Explicit deep always executes fresh six native roots even with empty changes. Heavy push with a valid deep receipt reruns those six serially within the invocation with coordinator host scheduling before accepting cached portable cells; a missing valid deep receipt still blocks with the normal verify:deep instruction rather than silently launching the full suite. This is a mode-specific fast path, never an exception allowing cached native acceptance. Retain and strengthen the existing docs-only assertions plus independent mode/cache mutations to prove this exact distinction.

## Bounded streaming reporter repair: both actual producers

Causal path: runTarget passes run:runLocalCommand into executeSuites, so local checks use tools/local-checks/process.mjs, not executeSuites' default runStreaming. The local data callback emits '[label] '+chunk; a leading LF produces the original synthetic trailing-space line. Correcting only test-runner/process.prefixStream would not fix this actual producer. Own both modules explicitly.

Export a small stateful line-prefix formatter from the lower tools/test-runner/process.mjs and use it in both that module's prefixStream and tools/local-checks/process.mjs. This is formatting reuse only, not replacing runLocalCommand with runStreaming: preserve local cleanEnvironment/GIT filtering, module interruption latch/active stops, per-stream byte limits, timeout/output-limit exit semantics, owned process-group kill and cleanup. Preserve generic streaming's separate truncation-marker/cap policy. Format only bytes already accepted by each existing runner's own limit policy; do not move caps into the shared helper or accept an offending local chunk.

Formatter must handle leading/consecutive LF and chunk boundaries: an empty logical line emits '[label]\n' without a synthetic trailing space; every nonempty line keeps its payload byte spelling, including deliberate spaces/tabs/CR. Do not trim child content. Prefix logical lines consistently in both producers. Preserve each API's existing final-unterminated-line policy explicitly (generic streaming appends newline; local runner previously did not). Flush accepted pending text before stop diagnostics and before completion so buffering cannot hide accepted output or reorder a timeout/output-limit/interruption message ahead of it. Retain bounded per-stream memory under existing cap and do not widen source/env/process control semantics. Existing old logs stay byte-exact; no new .gitattributes exemption.

The one new reporter literal exercises actual runStreaming child stdout/stderr blank/nonblank lines, fragmented chunks, final unterminated text and whitespace payload against independent exact strings. Strengthen existing 'P03 actual selected consumer failures skips and missing results cannot satisfy cached acceptance' lifecycle literal to run actual runTarget→executeSuites→runLocalCommand with an owned child that emits leading and consecutive LF, delayed chunks and both streams; inspect the resulting local raw log for correct prefixes and preserved payload, and retain its failure/skip/missing-result/cache assertions. Add direct local-command boundary assertions within those existing witnesses as needed: already accepted partial text before a stop diagnostic, unchanged output-limit result and clean snapshot/owner state. No literal name additions; total52 remains.

All8 existing process/execution assertions stay. Two grandchild test declarations use three-argument options that the recognizer intentionally rejects: convert only those declarations to simple two-argument async t callbacks with initial if(process.platform==='win32'){t.skip();return;}. Preserve exact names/order and original bodies below that guard. This maintains Windows skips and real Darwin/Linux work without widening P08. Actual frozen reporter acceptance requires all9 outcomes unskipped on its supported host.

No production cross-worktree heavy lease currently exists. The lease reference is disposable evidence, and P04 remains open. P03 serializes its own heavy invocation; coordinator serializes heavy invocations across worktrees according to docs/testing until P04 is implemented and accepted. Do not claim automatic host exclusion or bind a nonexistent lease into a passing receipt. No lease-source implementation is added to this eight-module package.

## Five reference registrations and parallel checkpoint

First retry implementation checkpoint may change only the owned matrix after successful activation: append literal tests_js/point_persistence_reference.test.mjs, tests_js/point_persistence_baselines.test.mjs, tests_js/point_paths_reference.test.mjs, tests_js/point_paths_parent_reference.test.mjs and tests_js/point_paths_digest_reference.test.mjs to existing node-workspace-other full suite. No glob-only future registration, new suite, new helper ownership, or S04/S05 source implementation here. Existing pattern expansion deduplicates within a suite. Keep all other matrix bytes intact.

This intentionally supersedes the previous external draft's absolute matrix-readonly statement only for these5 additive postactivation registrations. The retry base still adopts exact24615b matrix bytes; the new diff is honest scoped implementation. Freeze S04/S05 on the committed registration checkpoint before their isolated work begins, and hold matrix bytes fixed thereafter. Their audited sources/expected data remain disjoint and not P03-owned. S04/S05 native/default/parent/digest reference evidence cannot be manufactured by registering filenames.

## Verification and freeze order

Original evidence pin→refreshed documents/DAG/table/audit pin→fresh manifests→actual independent review→transition pin→activation, all normal hooks. Use only the reviewed P03-retry1-v2-prepare.mjs read-only bundle generator. Earlier P03-retry1-prepare.mjs is preserved and obsolete for this package. Concrete capture bundle has been separately reviewed; actual documents/manifests must be regenerated on their successive clean committed preparation bases. Proposed transition decision fields do not replace actual independent approval.

After source implementation, run five exact cells with registered names and independent omission/global/deletion/lock/policy/alias/source-emission mutations. Verify native obligations survive cache hits, empty deep changes, removed/added callers, and nonnative receipt reuse. Verify docs-only fast remains native-free. Run source-size and normal hooks; commit exact subject, capture fresh five raw logs, independently review full ordered identities and state. Original30/43 run remains unaccepted. CI parent and full migration remain open until their own criteria are met.
