# P08 platform literal recognition — draft for independent review

No code, repository metadata, task activation or acceptance is authorized by this external draft. The coordinator must independently review the actual plan/audit/manifests and activate a separately frozen task before implementation. This adds a precise recognizer capability; it does not retire or rewrite S08 or manufacture a passing source subject.

## Package, dependencies and ownership

New inert audit package P08, parent CI, owner/author jsonl_review, independent reviewer hooks_audit. P08.I depends on P07.V. P08.V depends on P08.I and P01.V. Append two tasks to the current338 effective DAG, yielding340. Add P08.V to never-frozen T05.I while preserving all existing dependencies, including P03.V and any evidence-reference additions. Check T05.I remains unfrozen at both transition freeze and activation. No other refinement or retirement. Preserve every existing catalog entry, accepted receipt, S08 source/log, historical DAG and P07 ownership chain. Ordinary validator successor rules still apply to the actual scoped edits.

Production ownership is exactly tools/migration/test-bindings.mjs, load-task-evidence.mjs and check.mjs. Direction stays check→loader→test-bindings; recognizer imports TypeScript only and never invokes loader or a test. Each file stays<=500; current sizes33/291/236 leave room. Tests: existing migration_test_bindings.test.mjs, migration_task_loader.test.mjs and migration_task_support.mjs only. The fixture receives a narrow optional conditional test-source/environment setup before its base commit; existing defaults and all10 loader tests remain unchanged in behavior. No new test/support file or matrix change: existing matrix explicitly includes binding test in node-runner-fast, loader in node-migration-evidence and support owned by both. Real Git lifecycle runs remain full-tier.

Own both future manifests and implementation.md/audit paths listed exactly in P08-audit-draft.json. No runtime emissions or product surfaces. Total20 literal tests:2existing+6new fast,10existing+2new lifecycle. Two180s/1MiB node-local/node-tap13 cells. Preserve existing names and assertions; new cases may share a literal only where declared in this frozen grouping.

## Public helper and caller wiring

Extend discoverTestIds(path, source, context?) with optional closed context {platform:string}. Omitted context preserves today's top-level-only discovery; no fallback to process.platform. Reject supplied malformed context or unknown platform strings; finite Node platform identifiers are win32,darwin,linux,aix,freebsd,openbsd,sunos,android. This list is only predicate evaluation support, not acceptance evidence for unexecuted platforms.

The accepted-subject loader supplies the environment platform from the frozen catalog environment selected by manifest cell.environmentId. Never infer platform from the reviewing host or receipt-supplied unvalidated data. Existing receipt validation must still require full command/environment equality, exact ordered raw TAP names/count, zero skips/fail/cancel/TODO/timeouts, output hash/bytes/time budgets and immutable tested source. check.mjs supplies its current process.platform explicitly for current-inventory static discovery. Other no-context consumers retain top-level behavior and tests.

Recognize only a top-level if whose condition is exactly process.platform === 'win32' or process.platform !== 'win32' (AST parentheses may be explicitly rejected; no normalization needed). Both branches must be explicit Blocks and contain only direct literal node:test declarations already accepted by the existing two-argument/nonempty callback rule. No else-if, nested conditionals, loops, options, computed names, property test.skip/only, dynamic imports or arbitrary expressions. Select only the matching branch for the supplied platform and retain order among surrounding top-level declarations. Duplicate selected declarations fail. Mutually exclusive equal names are not simultaneous duplicates; unknown/unsupported constructs must never supply names to satisfy a manifest. Source parse diagnostics fail before discovery. Imported default or named test aliases remain supported through exact node:test import provenance, never by spelling test alone.

## Shadow and mutation boundary

Conditional eligibility requires that process refers to the unshadowed Node global and test calls refer to actual node:test imports. Reject/leave undiscovered affected conditional declarations if a module/branch binding shadows process or the test alias, including import aliases, destructuring, function/class/var/let/const declarations; never descend into arbitrary scopes to collect test names. Inspect scopes without evaluating code. Test unrelated callback-local bindings separately so rejection cannot accidentally depend on the review host.

Known process mutations anywhere in the containing module invalidate conditional recognition: assignment/update/delete to process, process.platform or computed process properties; Object.defineProperty/defineProperties, Reflect.set/defineProperty/deleteProperty and Object.assign targeting process; writes through globalThis.process/global.process; direct process-object alias or process-object escape to another call. Reject relevant destructuring/alias definitions and imported local process identities rather than treating them as authoritative global values. Accessing process.platform or process.env for ordinary read-only executable selection remains supported. An unchanged predicate/callsite with a changed alias definition must not remain eligible. This is a closed syntactic guard, not a general JavaScript security analysis: arbitrary imported code may have side effects and is not claimed proven pure. Exact source/input provenance plus independently captured environment and observed TAP remains the execution evidence. Never eval source, execute predicates, monkeypatch process or infer Windows results from Darwin.

## Independent witnesses

Pure finite-table tests independently enumerate win32/darwin/linux outcomes for both operators and surrounding declarations. No-context returns only existing top-level names. Check wrong context shape/platform, parse errors, duplicates in selected branch, missing else, unknown predicate, computed property/name, nested branch/loop, options/skip, empty callback and fake/unbound test imports. Shadow and mutation fixtures change definitions while holding the original predicate/test call text constant; include both direct and known alias/reflective mutation negatives. Pure tests throw if executed callbacks are accidentally evaluated.

Bind three actual committed S08 test sources as immutable inputs: typed_json_oracle.test.mjs, raw_json_numeric.test.mjs and store_validation_ts.test.mjs. Read their bytes and compare exact names for both finite environments: normal default names python3, Windows alternatives python. Preserve unconditional3.12/3.13/3.14 and existing non-profile numeric/validation names. These are static Windows projection tests only. Current checkInventory wiring must be exercised rather than testing only an unused helper; use existing registry-discovery interface or a disposable minimal inventory, with explicit host context.

Actual lifecycle fixture freezes a literal conditional source and host environment before manifest creation, runs actual node --test in its disposable repository, commits exact output, and publishes a receipt only for the actual platform. Assert staged planning, activation, source, evidence and receipt boundaries. Validate all real raw identities independently of recognizer output. Negative same-count alternate-platform log uses an actual separately executed synthetic alternate declaration; bind its real bytes and then require rejection, not a fabricated passing receipt. Wrong frozen platform versus receipt platform, unsupported/skipped TAP, and source changed after immutable subject fail closed. Rebind tamper hashes when necessary so tests reach the intended environment/identity guard instead of failing only a stale hash first. Tests create all deliberate bad evidence in disposable Git, never modify historical real S08 evidence.

## Freeze and capture sequence

Integrate S08 exact source/log ancestry first through normal hooks; it remains unaccepted until this capability is accepted. Generator emits external unapplied bundles only. Prepare reviewed plan/audit/new DAG as exact pin-only documents and commit; freeze I/V manifests on that later clean base and commit; independently review; freeze transition and then activate exact catalog+lineage+review-lock normally. No broad metadata exemption. The generator refuses an unexpected DAG count, frozen T05.I, or changed three S08 input hashes.

Manifest inputs include the full actual local static import closure of all3modules and3tests/support, exact TypeScript package/toolchain/package-lock/tsconfig, matrix, three S08 source bytes, and all existing owned planning files. Resolve local imports strictly; unresolved relative imports are a blocker. The source-size baseline is bound. This tooling package can bind its transitive tooling inputs without freezing every product source.

After activation implement only owned files; run the two declared cells once the heavy slot is available, normal commit hooks, then fresh exactsubject capture. Independent reviewer validates source/context flow and exact rawlog identities/hash/environment, then I/V receipts. S08 retains its original frozen manifest/source/capture and receives its own separate validation only after P08 acceptance. No P03 completion or platform execution beyond actual capture is implied.
